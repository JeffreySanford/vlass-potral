# Aladin Dependency (MVP)

Status date: 2026-02-07

Aladin Lite is the only in-scope viewer runtime for MVP.

## MVP Usage
- Client-only initialization (no SSR execution)
- Pan/zoom/view-state serialization support
- Stable default survey configuration

## Deferred
- Mode B fallback integration (v2)
- In-app FITS proxy display pipeline (v2)

Keep this dependency pinned and tested through Nx test/e2e targets.
